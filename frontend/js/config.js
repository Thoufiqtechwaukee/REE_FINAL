// Frontend Configuration for REE-Final
// If hosting the frontend separately from the backend (e.g. on your IIS FTP host),
// set window.API_BASE_URL to your deployed Render URL (e.g., 'https://ree-final.onrender.com').
// Leave as empty string "" if the frontend and backend are served from the same server.

window.API_BASE_URL = window.API_BASE_URL || "https://ree-final.onrender.com";

